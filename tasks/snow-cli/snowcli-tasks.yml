version: '3'

tasks:

  sort-and-process-sql-folder:
    desc: Sorts and processes all SQL files in the given directory. Sorts and executes SQL files with numeric prefixes (e.g., 001-schema.sql)
    internal: true
    cmds:
      - python3 pyutil/snowclisp/snowclisp.py "{{.SQL_SORT_PROCESS_DIR}}" "{{.CLI_CONNECTION_NAME}}" --prefix-file sql/whoami.sql

  create-external-volume:
    desc: Creates a Snowflake external volume using the Snow CLI.
    internal: true
    vars:
      SQL_FILE: '{{.SQL_FILE | default "sql/init/create_external_volume.sql"}}'
    cmds:
      - cmd/create-external-volume.sh "{{.SQL_FILE}}"

  drop-external-volume:
    desc: Drops a Snowflake external volume.
    internal: true
    cmds:
      - cmd/drop-external-volume.sh "{{.EXTERNAL_VOLUME_NAME}}"

  desc-external-volume:
    desc: Describes a Snowflake external volume and outputs storage location JSON.
    internal: true
    vars:
      OUTPUT_DIR: '{{.OUTPUT_DIR | default "../../output"}}'
    cmds:
      - cmd/desc-external-volume.sh "{{.EXTERNAL_VOLUME_NAME}}" "{{.OUTPUT_DIR}}"

  run-init:
    desc: Runs unified Snowflake initialization script (conditionally handles managed/external storage).
    internal: true
    vars:
      SQL_DIR: '{{.SQL_DIR | default "sql/init"}}'
    cmds:
      - cmd/run-init.sh "{{.SQL_DIR}}"

  upload-files-to-internal-named-stage:
    desc: Uploads all files from the given directory to a Snowflake Internal stage using the Snowflake CLI and PUT command.
    internal: true
    vars:
      FILE_UPLOAD_DIR: '{{.FILE_UPLOAD_DIR | default "../../upload"}}'
      INTERNAL_NAMED_STAGE: '@{{.DEMO_DATABASE_NAME | default ""}}.{{.DEMO_SCHEMA_NAME_BRONZE | default ""}}.{{.DEMO_INTERNAL_NAMED_STAGE | default ""}}'
    cmds:
      - python3 pyutil/snowcliput/snowcliput.py "{{.FILE_UPLOAD_DIR}}" "{{.CLI_CONNECTION_NAME}}" "{{.INTERNAL_NAMED_STAGE}}"

  drop-database-if-exists:
    desc: Drops the specified Snowflake database if it exists using the Snowflake CLI.
    internal: true
    cmds:
      - snow sql --connection "{{.CLI_CONNECTION_NAME}}" --query "DROP DATABASE IF EXISTS {{.DEMO_DATABASE_NAME}};"

  generate-fleet-notebook:
    desc: Generates fleet analytics notebook from template with variable substitution.
    internal: true
    vars:
      TEMPLATE_FILE: '{{.TEMPLATE_FILE | default "notebook/fleet_analytics_notebook/templates/fleet_analytics_template.ipynb"}}'
      OUTPUT_FILE: '{{.OUTPUT_FILE | default "notebook/fleet_analytics_notebook/generated/fleet_analytics_notebook.ipynb"}}'
      VARIABLES_FILE: '{{.VARIABLES_FILE | default "notebook/fleet_analytics_notebook/variables.json"}}'
    cmds:
      - cmd/generate-notebook-generic.sh "{{.TEMPLATE_FILE}}" "{{.OUTPUT_FILE}}" "{{.VARIABLES_FILE}}"

  deploy-notebook:
    desc: Deploys a notebook to Snowflake using the Snow CLI.
    internal: true
    vars:
      PROJECT_DIR: '{{.PROJECT_DIR | default "notebook/fleet_analytics_notebook"}}'
    cmds:
      - cmd/deploy-notebook.sh "{{.PROJECT_DIR}}"

  stream-telemetry:
    desc: Streams simulated vehicle telemetry into Iceberg V3 table via Snowpipe Streaming SDK.
    internal: true
    vars:
      EVENT_COUNT: '{{.EVENT_COUNT | default "100"}}'
    cmds:
      - python3 pyutil/snowpipe_streaming/stream_telemetry.py --events {{.EVENT_COUNT}}
